# 模板匹配机制详解

本文档详细解释物体识别阶段的模板匹配机制。

## 概述

在识别阶段，系统需要将查询场景中检测到的物体与注册阶段建立的模板库进行匹配。由于每个物体实例可能有多个模板（N-shot learning），系统使用**聚合策略**来综合多个模板的相似度。

## 完整流程

### 场景设置

假设你已经注册了以下物体：
- **cellphone1**: 5个模板（从5张手持图像提取）
- **mug1**: 3个模板（从3张手持图像提取）

现在有一张查询场景图像，检测到了3个物体（proposals）。

### 步骤 1：特征提取

对查询场景中每个检测到的物体：
1. 使用检测框（bbox）定位物体
2. 使用 SAM2 分割物体区域
3. 使用 DINOv3-MGFA 提取特征向量（维度：768）

**结果**：3个查询特征向量 `[3, 768]`

### 步骤 2：相似度计算

系统会计算**每个查询物体**与**所有模板实例的所有模板**之间的相似度。

#### 相似度矩阵

生成一个三维相似度矩阵：`[N_proposals, N_instances, N_templates]`

在我们的例子中：
```
相似度矩阵形状：[3, 2, 5]
- 维度1: 3个检测物体
- 维度2: 2个实例 (cellphone1, mug1)
- 维度3: 最多5个模板（cellphone1有5个，mug1有3个）
```

#### 相似度计算方式

默认使用**余弦相似度**（Cosine Similarity）：

```python
# 对于每个查询物体 query_feat 和 cellphone1 的5个模板
similarities = []
for template in cellphone1_templates:  # 5个模板
    # 归一化特征
    query_norm = normalize(query_feat)
    template_norm = normalize(template)
    # 计算余弦相似度
    sim = dot_product(query_norm, template_norm)
    similarities.append(sim)

# 结果：[sim1, sim2, sim3, sim4, sim5]
```

**相似度范围**：[-1, 1]，1表示完全相似，-1表示完全相反

**示例**：
```
查询物体1 与各模板的相似度：
  cellphone1: [0.85, 0.82, 0.88, 0.79, 0.83]  # 5个模板
  mug1:       [0.45, 0.42, 0.48, 0.00, 0.00]  # 3个模板（补0）

查询物体2 与各模板的相似度：
  cellphone1: [0.52, 0.48, 0.51, 0.49, 0.50]
  mug1:       [0.78, 0.82, 0.75, 0.00, 0.00]

查询物体3 与各模板的相似度：
  cellphone1: [0.38, 0.35, 0.40, 0.36, 0.37]
  mug1:       [0.55, 0.52, 0.58, 0.00, 0.00]
```

### 步骤 3：聚合多模板相似度

由于每个实例有多个模板，需要将它们聚合为**单个实例级分数**。

系统支持三种聚合策略（默认使用 `max`）：

#### 策略 1: `max`（最大值，默认）

取该实例所有模板中的**最高相似度**作为实例分数。

**原理**：如果查询物体与该实例的任意一个模板非常相似，就认为匹配成功。

```python
# 对于查询物体1与cellphone1
similarities = [0.85, 0.82, 0.88, 0.79, 0.83]
instance_score = max(similarities) = 0.88
```

**示例结果**：
```
查询物体1的实例级分数：
  cellphone1: 0.88  (最大值来自模板3)
  mug1:       0.48  (最大值来自模板3)

查询物体2的实例级分数：
  cellphone1: 0.52  (最大值来自模板1)
  mug1:       0.82  (最大值来自模板2)

查询物体3的实例级分数：
  cellphone1: 0.40  (最大值来自模板3)
  mug1:       0.58  (最大值来自模板3)
```

#### 策略 2: `mean`（平均值）

取该实例所有模板的**平均相似度**。

```python
similarities = [0.85, 0.82, 0.88, 0.79, 0.83]
instance_score = mean(similarities) = 0.834
```

**优点**：考虑了所有模板的贡献，更稳定
**缺点**：可能被少数低质量模板拉低分数

#### 策略 3: `avg_k`（Top-K 平均）

取**前K个最高相似度**的平均值（默认 k=5）。

```python
similarities = [0.85, 0.82, 0.88, 0.79, 0.83]
top_k = sorted(similarities, reverse=True)[:3]  # k=3
instance_score = mean(top_k) = mean([0.88, 0.85, 0.83]) = 0.853
```

**优点**：平衡了稳定性和鲁棒性，排除了低质量模板

### 步骤 4：实例匹配

对每个查询物体，选择**实例级分数最高**的实例作为匹配结果。

```
查询物体1:
  cellphone1: 0.88 ← 最高分，匹配为 cellphone1
  mug1:       0.48

查询物体2:
  cellphone1: 0.52
  mug1:       0.82 ← 最高分，匹配为 mug1

查询物体3:
  cellphone1: 0.40
  mug1:       0.58 ← 最高分，匹配为 mug1
```

**最终结果**：
- 查询物体1 → cellphone1 (置信度: 0.88)
- 查询物体2 → mug1 (置信度: 0.82)
- 查询物体3 → mug1 (置信度: 0.58)

### 步骤 5：置信度过滤

系统会根据设置的阈值过滤低置信度的匹配（默认阈值 0.5）：

```
过滤后（threshold=0.5）：
✓ 查询物体1 → cellphone1 (0.88 >= 0.5)
✓ 查询物体2 → mug1 (0.82 >= 0.5)
✓ 查询物体3 → mug1 (0.58 >= 0.5)

过滤后（threshold=0.7）：
✓ 查询物体1 → cellphone1 (0.88 >= 0.7)
✓ 查询物体2 → mug1 (0.82 >= 0.7)
✗ 查询物体3 → 被过滤 (0.58 < 0.7)
```

## 完整数据流示意图

```
注册阶段：
┌─────────────────────────────────────────────┐
│ 手持图像1 → 特征提取 → cellphone1_template_1 │
│ 手持图像2 → 特征提取 → cellphone1_template_2 │
│ 手持图像3 → 特征提取 → cellphone1_template_3 │
│ 手持图像4 → 特征提取 → cellphone1_template_4 │
│ 手持图像5 → 特征提取 → cellphone1_template_5 │
└─────────────────────────────────────────────┘
                     ↓
         保存为 templates.pkl
              cellphone1: [5 x 768]
              mug1: [3 x 768]


识别阶段：
┌────────────────────────────┐
│ 查询图像                    │
│  ├─ 检测物体1 → 提取特征    │
│  ├─ 检测物体2 → 提取特征    │
│  └─ 检测物体3 → 提取特征    │
└────────────────────────────┘
             ↓
    [3 x 768] 查询特征
             ↓
┌─────────────────────────────────────────────────┐
│ 相似度计算（对每个查询物体）:                     │
│                                                 │
│ 查询物体1 vs cellphone1的5个模板 → [5个相似度]  │
│ 查询物体1 vs mug1的3个模板      → [3个相似度]  │
│                                                 │
│ 查询物体2 vs cellphone1的5个模板 → [5个相似度]  │
│ 查询物体2 vs mug1的3个模板      → [3个相似度]  │
│                                                 │
│ 查询物体3 vs cellphone1的5个模板 → [5个相似度]  │
│ 查询物体3 vs mug1的3个模板      → [3个相似度]  │
└─────────────────────────────────────────────────┘
             ↓
       聚合策略 (max)
             ↓
┌─────────────────────────────────────┐
│ 实例级分数:                          │
│                                     │
│ 查询物体1: cellphone1=0.88, mug1=0.48 │
│ 查询物体2: cellphone1=0.52, mug1=0.82 │
│ 查询物体3: cellphone1=0.40, mug1=0.58 │
└─────────────────────────────────────┘
             ↓
     选择最高分实例
             ↓
┌─────────────────────────────┐
│ 匹配结果:                    │
│ 物体1 → cellphone1 (0.88)   │
│ 物体2 → mug1 (0.82)         │
│ 物体3 → mug1 (0.58)         │
└─────────────────────────────┘
```

## 为什么使用多模板？

### 优点

1. **鲁棒性**：捕捉物体在不同视角、光照、姿态下的变化
2. **提高准确率**：只要查询物体与任意一个模板相似，就能匹配成功
3. **应对遮挡**：如果某些模板包含部分遮挡，其他清晰模板仍能提供有效匹配

### 示例场景

假设 cellphone1 的5个模板来自不同角度：
- 模板1: 正面
- 模板2: 侧面
- 模板3: 45度角
- 模板4: 轻微遮挡
- 模板5: 不同光照

查询场景中的手机是侧面视角 → 与模板2相似度最高 → 成功匹配

## 调整匹配策略

你可以通过修改 `ModelLoader` 来调整匹配策略：

```python
from src.model_loader import ModelLoader

loader = ModelLoader(device="cuda")

# 方式1: 使用不同的聚合策略
loader.matcher = TemplateMatcher(
    similarity_metric='cosine',  # 'cosine' 或 'euclidean'
    aggregation='avg_k',         # 'max', 'mean', 或 'avg_k'
    device='cuda'
)

# 方式2: 调整置信度阈值
recognizer = ObjectRecognizer(
    ...
    confidence_threshold=0.6  # 提高阈值减少误匹配
)
```

## 相关代码

- 相似度计算: `src/utils/matcher.py` → `TemplateMatcher.compute_similarity()`
- 聚合策略: `src/utils/matcher.py` → `TemplateMatcher.aggregate_template_scores()`
- 匹配流程: `src/recognition/object_recognizer.py` → `ObjectRecognizer.recognize()`

## 总结

**回答你的问题**：

> 当我注册 register_object.py 会对物体实例的多个图像建立模板，那么在匹配阶段，是将查询场景中每个物体的特征与所有模板特征计算相似度吗？

**是的！** 具体来说：

1. ✓ 计算查询物体与**每个实例的每个模板**的相似度
2. ✓ 使用聚合策略（默认 `max`）将多个模板相似度合并为实例级分数
3. ✓ 选择实例级分数最高的实例作为匹配结果
4. ✓ 根据置信度阈值过滤低置信度的匹配

这种设计确保了系统能够利用多视角模板的优势，提高识别的鲁棒性和准确率。
