# ============================================================
# Object Registration and Recognition System
# Core pip-installable dependencies
# ============================================================
#
# NOTE: Two external local packages must be installed separately
# after installing these pip dependencies:
#   1. Rex-Omni:  pip install -e models/Rex-Omni
#   2. SAM2:      pip install -e models/sam2
#
# These two CANNOT be listed here because they are local
# editable packages with build steps (CUDA extensions, etc.).
# ============================================================

# --- Deep Learning Framework ---
torch==2.7.0
torchvision==0.22.0

# --- Transformers / LLM ---
transformers==4.51.3
accelerate==1.10.1
flash-attn==2.7.4.post1
vllm==0.9.1 ; sys_platform != 'win32'
triton ; sys_platform != 'win32'

# --- Rex-Omni dependencies ---
matplotlib==3.10.6
numpy==1.26.4
Pillow==10.4.0
qwen_vl_utils==0.0.14
gradio==4.44.1
gradio_image_prompter==0.1.0
pydantic==2.10.6

# --- SAM2 dependencies ---
hydra-core>=1.3.2
iopath>=0.1.10
tqdm>=4.66.1
opencv-python-headless>=4.7.0

# --- Text Encoding (sentence-transformers) ---
sentence-transformers>=4.0.0

# --- Utilities ---
scipy>=1.10.0
scikit-learn>=1.0.0
